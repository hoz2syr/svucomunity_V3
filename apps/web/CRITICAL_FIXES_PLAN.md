# خطة عمل للإصلاحات الحرجة

## الإصلاحات المطلوبة:
1. حماية XSS في localStorage (تطبيق escapeHtml)
2. توحيد معالجة الأخطاء (استخدام handleLoginError)
3. تحقق من الجلسة مع الخادم (verifySessionWithServer)
4. إزالة تكرار DOMContentLoaded/event listeners
5. توحيد منطق المودال في shared.js
6. إنشاء صفحة account-locked.html
