---
name: Deploy Courses

on:
  push:
    branches: [main]
    paths:
      - 'apps/courses/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build placeholder
        run: echo "Courses app build step to be configured"
