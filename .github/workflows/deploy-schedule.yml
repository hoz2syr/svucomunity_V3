---
name: Deploy Schedule

on:
  push:
    branches: [main]
    paths:
      - 'apps/schedule/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build placeholder
        run: echo "Schedule app build step to be configured"
